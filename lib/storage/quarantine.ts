import 'server-only';

/**
 * Quarantine storage was removed with AI auto-moderation.
 * This stub satisfies imports while returning null (callers handle this gracefully).
 */
export async function getQuarantinePreviewUrl(_photoId: string): Promise<string | null> {
  return null;
}
